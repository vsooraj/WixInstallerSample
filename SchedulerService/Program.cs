namespace SchedulerService
{
    class Program
    {
        static void Main(string[] args)
        {
            //    HostFactory.Run(serviceConfig =>
            //    {
            //        serviceConfig.Service<ScheduleService>(serviceInstance =>
            //        {
            //            serviceInstance.ConstructUsing(() => new ScheduleService());
            //            serviceInstance.WhenStarted(execute => execute.Start());
            //            serviceInstance.WhenStopped(execute => execute.Stop());

            //        });
            //        serviceConfig.SetServiceName("WiXScheduler");
            //        serviceConfig.SetDisplayName("WiX Scheduler");
            //        serviceConfig.SetDescription("WiX Scheduler");
            //        serviceConfig.StartAutomatically();
            //    });
        }
    }
}
